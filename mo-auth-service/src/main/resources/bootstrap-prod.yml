spring:
  cloud:
    nacos:
      config:
        server-addr: 39.108.182.26:8848 #Nacos配置中心地址
        group: AUTH_GROUP
        file-extension: yml #文件拓展格式
        shared-configs: cust-auth-common.yml